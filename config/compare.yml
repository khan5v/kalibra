# AgentFlow — metrics and quality gates.
#
# Omit `metrics` entirely to run all built-in metrics.
# Omit `require` (or leave empty) for no quality gates.
#
# See config/examples/ for scenario-specific configurations.

# Run all built-in metrics (default when omitted).
# Uncomment and trim to run only specific ones:
# metrics:
#   - success_rate
#   - per_task
#   - cost
#   - steps
#   - duration
#   - tool_error_rate
#   - path_distribution
#   - token_usage
#   - token_efficiency
#   - cost_quality

require: []
