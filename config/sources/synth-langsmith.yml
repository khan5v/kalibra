# LangSmith synthetic traces — generated by scripts/synth_traces.py --target langsmith
# Usage:
#   kalibra pull @ls-baseline
#   kalibra pull @ls-current
#   kalibra compare --baseline @ls-baseline --current @ls-current

ls-baseline:
  source: langsmith
  project: synth
  since: 7d
  limit: 40
  tags: [kalibra, base0]

ls-current:
  source: langsmith
  project: synth
  since: 7d
  limit: 40
  tags: [kalibra, cur0]
