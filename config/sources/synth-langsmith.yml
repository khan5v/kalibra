# LangSmith synthetic traces — generated by scripts/synth_traces.py --target langsmith
# Usage:
#   agentflow pull @ls-baseline
#   agentflow pull @ls-current
#   agentflow compare --baseline @ls-baseline --current @ls-current

ls-baseline:
  source: langsmith
  project: synth
  since: 7d
  limit: 40
  tags: [agentflow, base0]

ls-current:
  source: langsmith
  project: synth
  since: 7d
  limit: 40
  tags: [agentflow, cur0]
